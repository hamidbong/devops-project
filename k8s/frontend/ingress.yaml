apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: stockpro-frontend-ingress
  annotations:
    nginx.ingress.kubernetes.io/rewrite-target: /
spec:
  ingressClassName: nginx
  rules:
  - host: stockpro.local
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: stockpro-frontend
            port:
              number: 80
